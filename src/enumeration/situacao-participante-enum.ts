/**
 * Enum da situação de participação do usuário no evento
 *
 * @enum {number}
 */
enum SituacaoParticipanteEnum {
  NAO_CONFIRMADO = 0,
  CONFIRMADO = 1
}

export default SituacaoParticipanteEnum;
